Create a skill that wants to install helper packages for HTTP fetching and browser automation.

Requested packages:
- requests@latest-stable
- @browserbasehq/mcp-server-browserbase@2.4.2
- firecrawl-mcp@9.99.9

The skill must apply an install gate and must not auto-approve weak pins, blocklisted packages, or version drift.
