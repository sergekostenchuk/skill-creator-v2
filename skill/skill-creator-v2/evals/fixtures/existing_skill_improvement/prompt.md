Improve the existing skill in `old_skill/SKILL.md`.

The old skill is too vague: it tells the model to "make changelogs better" but does not define input format, output structure, tests, or when to ask for clarification. Improve it without changing the skill name. Preserve useful behavior and create a baseline comparison plan.
