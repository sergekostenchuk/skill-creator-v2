Create a Codex skill from this workflow:

When I ask "summarize meeting notes into decisions", read a Markdown note, extract decisions, action items, owners, due dates, and unresolved questions. Output a clean Markdown summary. Keep it lightweight; do not create a multi-agent system unless there is a concrete need.

Input workflow details are in `input-workflow.md`.
