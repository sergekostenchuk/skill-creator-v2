Create a skill group for a research library workflow.

Skill A ingests and normalizes source notes. Skill B queries the normalized library and writes an answer with citations. They share references but should not write the same files. Define boundaries, shared state, handoff rules, and evals.
