Expected result:

- The skill declares Firecrawl MCP as a dependency.
- The skill includes a verification step before use.
- If Firecrawl is unavailable, it reports `tool_unavailable` or equivalent structured failure.
- Text-only fallback is allowed only with explicit limitation disclosure.
- No visual design claims are made without fetched/rendered evidence.
