Create a skill that analyzes visual design patterns from JavaScript-heavy gallery sites using a Firecrawl MCP dependency.

Assume the target runtime may not have Firecrawl connected. The skill must verify tool availability before relying on it and must not silently fall back to text-only fetches without warning.
