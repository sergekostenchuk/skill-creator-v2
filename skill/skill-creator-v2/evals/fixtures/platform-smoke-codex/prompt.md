Create a release smoke plan for installing `skill-creator-v2` into Codex.

The source package is already staged. The user has not approved runtime writes yet.
Show the exact safe sequence that proves the Codex runtime copy is current without silently changing any runtime root.
