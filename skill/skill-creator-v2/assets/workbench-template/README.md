# Workbench Template

Use this template for deep-path skill creation, high-risk skills, public release candidates, and skill groups.

Copy this directory into a project work area and rename it to `<skill-name>-workbench`.

Runtime packages should be built from `source/<skill-name>/`, not from the workbench root.
