# Eval Runtime Notes

`inline_fallback` checks are useful static evidence, but they are not full behavioral proof.

For strong behavioral claims, run independent executor outputs:

- `with_skill`
- `without_skill`
- `old_skill` when improving an existing skill

Save the actual outputs, grade them with evidence, aggregate benchmark results, and keep the diff visible.
